services:
  - type: web
    name: student-analysis-project
    env: python
    plan: free
    region: oregon
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    rootDir: .
    autoDeploy: true
